import { SlaStatusDirective } from './sla-status.directive';

describe('SlaStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new SlaStatusDirective();
    expect(directive).toBeTruthy();
  });
});
